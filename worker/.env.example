# BSNexus Worker Configuration (BSNEXUS_ prefix)
#
# Usage:
#   1. bsnexus-worker register --url <server> --token <reg-token>
#      → saves credentials below automatically
#   2. bsnexus-worker run
#      → starts worker using saved credentials

# Server connection
BSNEXUS_SERVER_URL=http://localhost:8000
BSNEXUS_REDIS_URL=redis://localhost:6379

# Registration token (from the admin UI → Workers → Registration Token)
BSNEXUS_REGISTRATION_TOKEN=

# Worker identity
BSNEXUS_WORKER_NAME=
BSNEXUS_EXECUTOR_TYPE=claude-code

# Runtime
BSNEXUS_HEARTBEAT_INTERVAL=30
BSNEXUS_DURATION=

# Registered credentials (auto-generated by 'bsnexus-worker register')
BSNEXUS_WORKER_ID=
BSNEXUS_WORKER_TOKEN=
